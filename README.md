use npm install in terminal to download required libraries
use npm run dev for 2 folder backend and frontend
this project use mysql for database
create db_name=fastclinic with this command: 
CREATE DATABASE fastclinic CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
in ur database and configuration env file with ur database 
dont 4get to run node src/seed.js to get demo account
